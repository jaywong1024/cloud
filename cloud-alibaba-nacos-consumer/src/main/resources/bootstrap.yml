server:
  port: @consumer.port@
spring:
  # 应用信息
  application:
    name: @module.alibaba.nacos.consumer@
  # 服务注册
  cloud:
    nacos:
      discovery:
        server-addr: @nacos.host@:@nacos.port@
      config:
        server-addr: @nacos.host@:@nacos.port@
        file-extension: yaml